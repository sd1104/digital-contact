.infoArea
  .infoArea__infoHead
    =render 'contact_books/headButtonLinks'
    .infoArea__infoHead__headBox
  .infoArea__infoMenu
    %ul.breadCrumbsLink
      %li
        - if !current_user.room_id?
          サイドバーからクラスを登録して下さい。クラス登録後再度ログインすることで、利用が可能になります。
        - else
          クラスを選択して下さい。