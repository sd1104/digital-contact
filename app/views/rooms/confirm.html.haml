.SchoolWrapper
  = render partial: "/rooms/top_nav"
  .MainNav
    .MainNav__Wrapper
      .RoomEditSelect
        .roomsConfirm
          .roomsConfirm__head
            １年
          - @rooms.where(grade: 1).each do |room|
            .room
              .room__name
                = room.number
                = link_to '編集', edit_room_path(room), class: "roomEditLink"
                = link_to '削除', room_path(room), method: :delete, class: "roomEditLink"
        .roomsConfirm
          .roomsConfirm__head
            ２年
          - @rooms.where(grade: 2).each do |room|
            .room
              .room__name
                = room.number
                = link_to '編集', edit_room_path(room), class: "roomEditLink"
                = link_to '削除', room_path(room), method: :delete, class: "roomEditLink"
        .roomsConfirm
          .roomsConfirm__head
            ３年
          - @rooms.where(grade: 3).each do |room|
            .room
              .room__name
                = room.number
                = link_to '編集', edit_room_path(room), class: "roomEditLink"
                = link_to '削除', room_path(room), method: :delete, class: "roomEditLink"
        .roomsConfirm
          .roomsConfirm__head
            ４年
          - @rooms.where(grade: 4).each do |room|
            .room
              .room__name
                = room.number
                = link_to '編集', edit_room_path(room), class: "roomEditLink"
                = link_to '削除', room_path(room), method: :delete, class: "roomEditLink"
        .roomsConfirm
          .roomsConfirm__head
            ５年
          - @rooms.where(grade: 5).each do |room|
            .room
              .room__name
                = room.number
                = link_to '編集', edit_room_path(room), class: "roomEditLink"
                = link_to '削除', room_path(room), method: :delete, class: "roomEditLink"
        .roomsConfirm
          .roomsConfirm__head
            ６年
          - @rooms.where(grade: 6).each do |room|
            .room
              .room__name
                = room.number
                = link_to '編集', edit_room_path(room), class: "roomEditLink"
                = link_to '削除', room_path(room), method: :delete, class: "roomEditLink"
        .roomsConfirm
          .roomsConfirm__head
            その他
          - @rooms.where(grade: 7).each do |room|
            .room
              .room__name
                = room.number
                = link_to '編集', edit_room_path(room), class: "roomEditLink"
                = link_to '削除', room_path(room), method: :delete, class: "roomEditLink"
      = link_to '戻る', control_rooms_path, class: "backLink"
