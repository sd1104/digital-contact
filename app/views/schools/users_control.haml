.SchoolWrapper
  .TopNav
    .Left
      .Left__userBox
        %h6 ログイン中のユーザー
        .Left__userBox__userName 
          = current_school.school_admin
      .Left__logOutBox
        = link_to 'schoolLogOut', destroy_school_session_path, method: :delete, class: "logOutBtn"
    .Right  
      .Right__infoHead
        .Right__infoHead__userschool
          %h6 学校名
          .Right__infoHead__userschool__schoolName
            = current_school.school_name
        .Right__infoHead__headBox
      .Right__infoMenu
        %ul.breadCrumbsLink
          %li
            以下よりクラスを選んでください。
  .MainNav
    .MainNav__userSelect
      .MainNav__userSelect__head
        ク ラ ス 選 択
      %ul.dropdwn.orderList
        %li.list
          1年
          %ul.dropdwn_menu.orderList
            - @rooms.each do |room|
              %li.list
                = link_to room_show_rooms_path(room), class: "dropLink" do
                  = room.grade
        %li.list
          2年
          %ul.dropdwn_menu.orderList
            %li.list
              - @rooms.where(grade: 2).each do |room|
                = link_to room_show_rooms_path(room), class: "dropLink" do
                  = room.number
        %li.list
          3年
          %ul.dropdwn_menu.orderList
            %li.list
              - @rooms.where(grade: 3).each do |room|
                = link_to room_show_rooms_path(room), class: "dropLink" do
                  = room.number
        %li.list
          4年
          %ul.dropdwn_menu.orderList
            %li.list
              - @rooms.where(grade: 4).each do |room|
                = link_to room_show_rooms_path(room), class: "dropLink" do
                  = room.number
        %li.list
          ５年
          %ul.dropdwn_menu.orderList
            %li.list
              - @rooms.where(grade: 5).each do |room|
                = link_to room_show_rooms_path(room), class: "dropLink" do
                  = room.number
        %li.list
          ６年
          %ul.dropdwn_menu.orderList
            %li.list
              - @rooms.where(grade: 6).each do |room|
                = link_to room_show_rooms_path(room), class: "dropLink" do
                  = room.number
        %li.list
          その他
          %ul.dropdwn_menu.orderList
            %li.list
              - @rooms.where(grade: 7).each do |room|
                = link_to room_show_rooms_path(room), class: "dropLink" do
                  = room.number